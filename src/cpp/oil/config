#
# config
#

#
# OBS.: O ativamento do verbose deve ser feito dentro do makefile ou
# config.mak no caso para o tecmake.
#

include ${OPENBUS_HOME}/config

LUA51LIB=${LUA51}/lib

TOLUA_INC=${TOLUA}/include
TOLUA_LIB=${TOLUA}/lib

# Somente necessário para a execução da base de testes
CXXTEST_INC=${CXXTEST}

FLAGS=-Wall -ansi -O3

